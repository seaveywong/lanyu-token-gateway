{
  "name": "lanyu-token-gateway",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter @lanyu/admin-web dev",
    "dev:portal": "pnpm --filter @lanyu/portal-web dev",
    "build:admin": "pnpm --filter @lanyu/admin-web build",
    "build:portal": "pnpm --filter @lanyu/portal-web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
